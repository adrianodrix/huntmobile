import { StatusBar } from 'react-native'

StatusBar.setBackgroundColor('#DA552F', true)
StatusBar.setBarStyle('light-content')
StatusBar.setNetworkActivityIndicatorVisible(true)
StatusBar.setTranslucent(true)
